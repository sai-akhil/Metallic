# generate random floating point values

import random
# seed random number generator

# generate random numbers between 0-1
for _ in range(82):
	value = random.uniform(0.01, 0.68)
	print(round(value,2))

